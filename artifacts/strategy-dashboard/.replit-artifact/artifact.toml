kind = "web"
previewPath = "/"
title = "Lane B Strategy Review Dashboard"
version = "1.0.0"
id = "artifacts/strategy-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22851

[services.development]
run = "pnpm --filter @workspace/strategy-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/strategy-dashboard", "run", "build" ]
publicDir = "artifacts/strategy-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22851"
BASE_PATH = "/"
