# Refined Replit Prompt

Use the provided React + Vite app only. Build an internal operator-facing Lane B review dashboard from the bundled static files under `public/data/` and `public/downloads/`. Do not add a backend, do not fetch raw repo paths, and do not change the six page ids: `overview`, `strategy`, `review`, `package`, `batch`, `handoff`.

Apply the hybrid design authority exactly:
- Modal-Nodal owns the shell, dark neutral surface language, and signal-accent posture.
- ThetaFrame owns information hierarchy, lane structure, and semantic status treatment.
- Yuki owns roadmap/selectable-function layout patterns.
- Flipper owns fail-closed tone and local-first control language.

Keep these distinctions visually explicit:
- promoted canonical alias
- latest successful run
- review recommendation
- latest package
- latest batch

Use the bundled dashboard contract as truth:
- `public/data/dashboard_index.json`
- required datasets must fail closed with a blocking error state
- optional datasets must render `Unavailable`

Do not redesign the IA. Do not rename files. Do not replace the static-data model. Focus on making the existing scaffold feel intentional, premium, and review-ready while preserving all manifest- and package-driven semantics.
